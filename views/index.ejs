<% include ./partials/boilerplate.ejs %>

<div>
  <h1><%= message %></h1>
  <h3><%= subTitle %></h3>
</div>

<div>
<h4>Dresselhaus Quotes is a compilation of favorite quotes from the students of NYC-WDI-DRESSELHAUS. It's super cool and great.</h4>
</div>

<p>We have quotes by <% quoteAuthors.forEach(function(author, index) { %>
  <% if (index !== quoteAuthors.length - 1) { %>
    <%= author %>,
  <% } else { %>
    <%- author -%>
  <% } %>
<% }) -%>.</p>

<a href='/quotes'>See all quotes</a>

<% include ./partials/footer.ejs %>
